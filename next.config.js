/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Turbopack is enabled by default; adding an empty turbopack config silences the warning
  turbopack: {},
};
module.exports = nextConfig;
